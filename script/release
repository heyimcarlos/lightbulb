#!/usr/bin/env bash

echo "The inherited OpenCode release helper is disabled in Lightbulb because publish.yml was removed."
echo "Design and verify a Lightbulb-native release workflow before re-enabling this command."
exit 1
