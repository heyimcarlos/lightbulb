# Long-Running Goal: Architecture Decisions and Issues

You are an xhigh Codex architecture worker. Do not implement product code.

Use the Matt Pocock-style workflow already configured in `docs/agents/`.

Outputs:

- Refine PRD/ADRs if needed.
- Create or update `docs/lightbulb/designs/bootstrap-spine-design.md`.
- Create or update `docs/lightbulb/designs/bootstrap-spine-design.html`.
- Propose GitHub issue bodies under `docs/lightbulb/plans/issues/` for vertical slices.
- Leave final report in `.lightbulb/runs/architecture-final.md`.

Focus on vertical slices:

1. Durable schema tracer bullet.
2. Dashboard tracer bullet.
3. Worker/artifact flow tracer bullet.
4. Gate/context policy tracer bullet.
5. Supervisor/observability tracer bullet.
